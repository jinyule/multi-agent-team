# 仓库工作规则

本项目采用独立 macOS 桌面 + Go 本机服务。修改前阅读 [架构](docs/architecture.md)；工程依据与适配理由见 [deepseek-harness 分析](docs/reference/deepseek-harness-engineering.md)。参考仓库只读，不能将 Cordis、全插件化或其组织专属政策当作本项目要求。

## 设计与实现

- 服务拥有任务、决策和持久化；桌面只通过有限 IPC 调用服务。不得让 renderer 直接访问文件系统、数据库、shell 或 Node。
- 在 JSON、文件、配置、进程和持久化入口校验数据；错误显式返回。业务数据、请求去重记录和审计事件在同一事务提交。
- 资源由创建者回收。取消/退出必须等待进程和请求停止；请求取消不等于副作用已经撤销。超时、信号、退出码分别记录。
- 成员身份不依赖模型。引擎能力缺失必须明确显示，不得以测试替身、静态页面或模型自述冒充真实执行。
- 模型输入、工具动作、目标版本与结果保留必要记录；不能记录密钥或隐藏思维。配置/授权不能由不可信仓库文本自行提升。
- 新增依赖必须说明目的、固定版本并更新 lockfile。不复制参考仓库源码、品牌或许可证；维护依赖清单及分发许可证。
- 不吞掉未知错误、不为测试加入全局跳过开关。删除死代码优先于为无意义代码补覆盖率。

## 开发与验证

- 行为修改先添加能暴露缺口的测试并观察有效 RED，再实现并跑 GREEN。环境未准备好不是有效 RED。原始失败记录不能覆盖。
- 根据 [测试规范](docs/testing.md)选测试层：纯逻辑、真实 SQLite/HTTP 集成、真实进程、真实 Electron 窗口、打包产物。只替换昂贵或不确定的外部模型边界。
- 用户可见行为必须增加/更新真实窗口场景和语义快照。CI 只比较快照，不自动更新。断言真实文件/数据库/API/UI 状态，不能只看 Agent 的成功文字。
- `make quick` 检查格式、类型、vet 和规则；`make test` 跑 Go race；`make coverage` 强制逐文件阈值；`make e2e` 跑真实入口；`make check` 为完整本地/CI 门禁。详见 [工程规范](docs/development.md)。
- 改门禁要测试它能拒绝坏输入、缺失结果和跳过检查。必需检查不允许 `continue-on-error`、静默 skip 或空测试集合通过。
- 修改已有通过的代码后重跑相关检查；未改相关内容不因提交/推送反复跑同一套。仓库范围的规范/CI 改动需跑完整门禁。
- 非机械改动更新当前事实文档，并新增或更新 ADR/Agent Note，说明问题、决定、真实备选与代价。文档在仓库中使用相对链接；历史本机证据不作为 CI 门禁证据。

## 提交、合并与发布

- 分支开发、PR 交叉检视，作者不能批准自己的变更；每次合入主干由人最终决定。不得擅自降低门禁、扩大范围或发布。
- PR 附目标、验收条件、实际验证、失败/限制和决策记录。按 `feat/fix/docs/test/refactor/build/ci/chore` 使用有意义的提交标题。
- 本地 hook 是便利检查，不能代替 CI。禁止强制覆盖他人变化；需要重写时仅对明确负责的分支使用 lease。
- CI 无秘密执行必需测试；真实模型测试独立且显式启用，缺凭据必须报告未验证，不能充当通过。
- 发布规则见 [release](docs/release.md)：手动触发、版本/提交校验、先构建验证、后上传同一组字节。当前未签名开发包只能作为开发预览，不能称正式分发就绪。
- 当前仓库无已配置远端时不得声称线上 CI、分支保护或发布环境已经生效。
