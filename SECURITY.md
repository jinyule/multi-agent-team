# 安全与数据处理

当前产品是本机开发预览。Unix socket 与状态目录为 owner-only，但同一 OS 用户下的任意进程仍可能读取它们；这不是针对同权限恶意 Agent 的隔离。真实引擎上线前必须验证执行沙箱、控制接口不可达和秘密隔离，见[引擎接入契约](docs/engine-contract.md)。

不提交 `.env`、API key、登录凭据、数据库、录屏中的个人信息或模型秘密。测试只继承必要环境，创建私有临时目录，回收自己拥有的进程。不可通过修改测试前提、删除用户数据或关闭系统保护绕过失败。

CI 的 PR 检查不读取秘密。发布写权限与凭据仅限人工批准的发布步骤；上游依赖固定版本，经同样门禁升级。workflow 和 gate 变更必须独立人工检视，脚本本身不能替代信任配置。

当前无公开安全邮箱或仓库入口，尚未建立对外漏洞接收渠道；通过实际项目维护者的私密渠道报告即可，不在公开 Issue 放可用凭据。将来设置远端时补充真实联系方式。
