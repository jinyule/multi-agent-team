# 实施计划与能力状态

架构：方案二，用户已确认。每个里程碑完成后记录实际验证，不把计划列为已实现能力。

| 里程碑 | 可验收结果 | 当前状态 |
| --- | --- | --- |
| M0 架构决策 | ADR、模块边界、接口和质量/恢复契约 | 已形成 |
| M1 独立服务与桌面 | 注册项目、提交目标、固定成员、持久事件、服务重启保留状态，桌面可独立退出 | 已完成本机开发验收，见 [M1 验证记录](verification/m1.md) |
| M2 双引擎接入 | 同轮交付 Codex 与 Claude Code 的会话、事件、审批、中断、恢复与能力清单；逐个适配器验证 | 待实现 |
| M3 团队调度 | 需求/验收、计划依赖、成员交接、并行工作区、预算与故障核对 | 待实现 |
| M4 质量与 Git 交付 | 可核验 TDD、独立测试和检视、CI 修复、绑定版本的人工合并决定 | 待实现 |
| M5 nano-harness 闭环 | 完成外部控制接口真实开发验收，接入自建引擎，准备发布候选 | 待实现 |
| M6 桌面分发 | 安装、升级、用户可控的后台服务启动、平台兼容性与数据迁移 | 待实现 |

M1 先写行为测试并保留 RED/GREEN，覆盖重复请求、持久化、缺失项目、快照/事件一致性、第二实例拒绝启动和真实 binary 重启。桌面验证实际窗口、服务连接、提交目标与重连；没有真实引擎时目标明确显示等待接入。

M1 已通过 `make check` 和 `go vet ./...`，真实 Electron 窗口截图已检查。当前没有开启真实模型调用、注册系统后台服务、连接代码托管或修改 nano-harness；这些不是 M1 验收通过所代表的能力。

M1 后已建立本仓库工程基线：规则负向测试、逐文件覆盖率、Go/桌面反空与反跳过、GitHub CI 汇总、真实开发包 E2E 和字节核验，见[工程门禁验证](verification/engineering-gates.md)。这是维护本仓库的工具，M4 中平台为用户项目编排质量与交付的能力仍待实现；未签名开发 zip 也不等于 M6 正式分发完成。

后续门禁增加并发预算竞争、旧结果迟到、真实工具审批与取消、门禁篡改、授权过期、远端不确定结果等验证。单个案例通过不能替代长期成功率评估。
