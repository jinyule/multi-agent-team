# 开发与贡献

本仓库目前为早期个人项目。先阅读 [AGENTS](AGENTS.md)、[工程规范](docs/development.md)、[测试要求](docs/testing.md) 和[发布规则](docs/release.md)。

在独立分支中提出明确目标和验收条件，先测试后实现。非机械变更更新相应当前事实文档，并记录 ADR/Agent Note；解释真实选项和代价，不记录模型思维过程。

提交标题使用 `feat`、`fix`、`docs`、`test`、`refactor`、`build`、`ci` 或 `chore`，正文在必要时说明原因。PR 按模板提供真实命令结果、语义快照/截图、未测项和风险，不以截图代替测试门禁。没有 Issue 时无需虚构编号。

推送前 `make check`，发布相关改动还需 `make package`；依赖变更保持 lockfile、许可证与能力声明一致。不得自动刷新预期快照、跳过失败测试或降低门禁来获取绿灯。

变更由非作者检视后，人最终决定是否合入 main。当前 Agent 团队执行能力尚未接入，因此不能声称本次本地自检已经完成 Agent 交叉评审。发布为另一个明确决定。

报告缺陷时提供最小复现、预期/实际行为、运行版本和脱敏日志。不要提交凭据、个人项目内容或本地状态库；无秘密复现优先。产品许可证尚未决定，不能从依赖许可证推导本项目的分发许可。
